import { Heading, Text, VStack } from "@chakra-ui/react";

export function MapPage() {
	return (
		<VStack align="stretch" gap="3">
			<Heading size="lg">Map</Heading>
			<Text color="fg.muted">Interactive city map module entry point.</Text>
		</VStack>
	);
}

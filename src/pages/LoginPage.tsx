import { Heading, Text, VStack } from "@chakra-ui/react";

export function LoginPage() {
	return (
		<VStack align="stretch" gap="3">
			<Heading size="lg">Login</Heading>
			<Text color="fg.muted">Authentication login page placeholder.</Text>
		</VStack>
	);
}
